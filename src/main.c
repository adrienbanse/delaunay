#include <stdio.h>
#include <stdlib.h>
#include "inputs.h"
#include "delaunay.h"

int main(){
    // Point* p1 = (Point*) malloc(sizeof(Point));
    // Point* p2 = (Point*) malloc(sizeof(Point));
    // Point* p3 = (Point*) malloc(sizeof(Point));
    // Point* p4 = (Point*) malloc(sizeof(Point));
    // p1->x = -1; p1->y = 0;
    // p2->x = 0; p2->y = 1;
    // p3->x = 1; p3->y = 0;
    // p4->x = 0; p4->y = -1;

    // Triangle* tr1 = (Triangle*) malloc(sizeof(Triangle));
    // Triangle* tr2 = (Triangle*) malloc(sizeof(Triangle));
    // Vertex* ve1 = (Vertex*) malloc(sizeof(Vertex));
    // Vertex* ve2 = (Vertex*) malloc(sizeof(Vertex));
    // Vertex* ve3 = (Vertex*) malloc(sizeof(Vertex));
    // Vertex* ve4 = (Vertex*) malloc(sizeof(Vertex));
    // Mesh* mesh = (Mesh*) malloc(sizeof(Mesh));
    // // construct vertices
    // ve1->root = tr1; ve1->p = p1;
    // ve2->root = tr1; ve2->p = p2;
    // ve3->root = tr2; ve3->p = p3;
    // ve4->root = tr2; ve4->p = p4;
    // // construct triangles
    // tr1->t1 = tr1; tr1->t2 = NULL; tr1->t3 = NULL;
    // tr1->v1 = ve1; tr1->v2 = ve2; tr1->v3 = ve4;
    // tr2->t1 = tr2; tr2->t2 = NULL; tr2->t3 = NULL;
    // tr2->v1 = ve2; tr2->v2 = ve3; tr2->v3 = ve4;
    // // construct mesh
    // Triangle** t_list = (Triangle**) malloc(2 * sizeof(Triangle*));
    // t_list[0] = tr1; t_list[1] = tr2;
    // Vertex** v_list = (Vertex**) malloc(4 * sizeof(Vertex*));
    // mesh->t_list = t_list;

    return EXIT_SUCCESS;
}